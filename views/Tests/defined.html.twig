{% extends 'index.html.twig' %}

{% block sample %}
    <p>
        {% if var is not defined and foo.method(var) is not defined %}
            This variables not defined
        {% endif %}
    </p>
{% endblock sample %}
