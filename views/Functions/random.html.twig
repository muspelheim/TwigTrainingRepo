{% extends 'index.html.twig' %}

{% block code %}
{% verbatim %}
    <p>
        {{ random(['apple', 'orange', 'citrus']) }}
        {{ random('ABC') }}
        {{ random() }}
        {{ random(5) }}
    </p>
{% endverbatim %}
{% endblock code %}

{% block sample %}
    <p>
        {{ random(['apple', 'orange', 'citrus']) }}
        {{ random('ABC') }}
        {{ random() }}
        {{ random(5) }}
    </p>
{% endblock sample %}
