{% extends 'index.html.twig' %}

{% block sample %}
    <p>
        {{ random(['apple', 'orange', 'citrus']) }}
        {{ random('ABC') }}
        {{ random() }}
        {{ random(5) }}
    </p>
{% endblock sample %}
